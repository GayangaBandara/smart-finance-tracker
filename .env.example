# Copy this file to .env.local and fill values (do NOT commit .env.local)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_...  # public anon key
# For server-only (do not expose to browser):
# SUPABASE_SERVICE_ROLE_KEY=sb_secret_...  # server-only; use in backend functions
